import { AlunosMatriculadosImpuroPipe } from './alunos-matriculados-impuro.pipe';

describe('AlunosMatriculadosImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new AlunosMatriculadosImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
