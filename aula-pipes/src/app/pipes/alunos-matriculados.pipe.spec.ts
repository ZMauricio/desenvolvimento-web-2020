import { AlunosMatriculadosPipe } from './alunos-matriculados.pipe';

describe('AlunosMatriculadosPipe', () => {
  it('create an instance', () => {
    const pipe = new AlunosMatriculadosPipe();
    expect(pipe).toBeTruthy();
  });
});
